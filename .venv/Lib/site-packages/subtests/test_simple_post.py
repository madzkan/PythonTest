import time

from addons.swipe_and_find_element import SwipeAndFindElement
from addons.image_comparison import ImageComparison
from selenium.webdriver.common.by import By
from src.testproject.classes import DriverStepSettings, StepSettings
from src.testproject.decorator import report_assertion_errors
from src.testproject.enums import SleepTimingType
from src.testproject.sdk.drivers import webdriver
from subtests import test_logintest
import pytest


@report_assertion_errors
def test_activity_simple_post(driver):
    # Test Parameters
    LoginTest = "Login"
    Activity = "Activity"
    TestComment = "comment"

    time.sleep(7)

    # 4. Click 'Add +'
    add_post = driver.find_element(By.XPATH, "//android.view.ViewGroup[1]/android.view.ViewGroup[1]/android.view.ViewGroup[1]/android.view.ViewGroup/android.widget.ImageView")
    add_post.click()

    time.sleep(3)

    # 10. Click 'Write here or use @ to mention someone.'
    #write_here_or_use_to_mention_someone_ = driver.find_element(By.XPATH,  "//android.widget.EditText[@text = 'Write here or use @ to mention someone.']")
    #write_here_or_use_to_mention_someone_.click()

    # 11. Type 'This is a test post' in 'Write here or use @ to mention someone.'
    #driver.switch_to_frame(By.XPATH, "//android.webkit.WebView[@text = 'CN-Editor']")
    add_text = driver.find_element(By.XPATH, "//android.widget.EditText")
    add_text.click()
    #add_text.send_keys("test")
    driver.press_keycode(29)
    driver.press_keycode(30)
    driver.press_keycode(31)

    # 12. Click 'Post'
    post = driver.find_element(By.XPATH, "//android.widget.TextView[@text = 'Post']")
    post.click()

    time.sleep(10)